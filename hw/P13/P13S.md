# Lecture 13 Problems - Solutions

1. 
    $$
    x_{cam} = d K_L^{-1} \begin{bmatrix}x_{img}^L \\ y_{img}^L\\1\end{bmatrix}
    $$

    $$
    x_{world} = 
    \begin{bmatrix}
    R_L &t_L \\
    0  & 1
    \end{bmatrix}^{-1}
    \begin{bmatrix}x_{cam} \\ 1\end{bmatrix}
    $$

2. 
    $$
    K_R [R|t]_R \begin{bmatrix}x_w \\ y_w \\ z_w \\1\end{bmatrix}
    $$

----

3. $1x + 1y + 0 = 0$

    $ \rightarrow y = -x$

4. $2x + 1y - 400 = 0$

    $\rightarrow \begin{bmatrix}2\\1\\-400\end{bmatrix}$

5. Suppose their dot product is zero. Then:
    $$
    \begin{align*}
    \ell \cdot p = 0 = ax + by + cw
    \end{align*}
    $$
    Dividing both sides by $w$ gives $a(x/w) + b(y/w) + c = 0$, which according to the line equation tells us that the point is on the line.

6. $[70, 70, 1] \times [0, 40, 1] = [  30,  -70, 2800]$

7. $[2, 1, -400] \times [  30,  -70, 2800] = [148.2, 103.5,   1.        ]$​

    Or, un-normalized, it's $[-25200, -17600,   -170]$

8. $ax + by + c = 0$

    $by = -ax - c$

    $y = -(a/b)x - (c/b)$

    The slope is $-a/b$.

9. 
    $$
    \frac{a_1}{b_1} = \frac{a_2}{b_2}
    $$

10. 
    $$
    \begin{bmatrix}
    b_1 c_2 - b_2 c_1 \\
    a_2 c_1 - a_1 c_2 \\
    a_1 b_2 - a_2 b_1
    \end{bmatrix}
    $$

11. 
    $$
    \frac{a_1}{b_1} = \frac{a_2}{b_2}\\
    a_1 b_2 = a_2 b_1\\
    a_1 b_2 - a_2 b_1 = 0
    $$

12. In 3-space, such a point is a vector from the origin parallel to the projection plane that never intsersects it. These represent points infinitely far in a direction, or simply direction vectors.
